# AI 跑团 GM — 领域语境 (CONTEXT)

单人 AI 跑团 / 互动叙事「游戏主持人(GM)」的领域语言。玩家自由文本输入，AI 即兴叙事并分饰 NPC，但剧情推进被编剧预设的状态机白名单约束。本文件只记录领域专家级别的术语，不耦合实现细节（DDL/接口以两份设计文档的基线为准）。

## Language

### 玩法核心

**节点 / Node**：
状态机的一个离散关卡（一处场景 / 一个剧情状态），玩家在任一时刻恰好处于一个节点。
_Avoid_: 关卡（口语）、页面

**回合 / Turn**：
玩家一次自由文本输入 + AI 一次结构化响应（叙事 / NPC 对白 / 状态变更 / 跳转提议）所构成的一次交互。
_Avoid_: 对话轮、step

**跳转 / Transition**：
从当前节点指向另一节点的有向边，由 condition 把守；既是剧情推进的离散闸门，也是约束 AI 的**白名单**来源。
_Avoid_: 切换、流转

**停留 / Stay**：
一个回合内未发生节点跳转（`proposedTransition` 为 null 或被白名单拒绝），玩家仍在原节点；其特征是**状态可变、节点不变**。
_Avoid_: 原地踏步、卡住

**结局 / Ending**：
标记为 `is_ending` 的终止节点（WIN 或 LOSE 类型）；玩家经一次白名单跳转进入它，对局即结束、status 置 WIN/LOSE。**结局只由编剧建模、经白名单产生，引擎无特例死法。**
_Avoid_: 通关、game over（口语）

### 角色与对局

**玩家 / PLAYER**：
游玩已发布剧本的角色（开局、提交回合）；注册默认授予。
_Avoid_: 用户（user 指账号，player 指游玩角色）

**编剧 / AUTHOR**：
创建 / 编辑 / 发布自己剧本的角色，负责状态机建模（节点 / NPC / 分支 / condition）；由 ADMIN 授予。
_Avoid_: 作者、creator

**管理员 / ADMIN**：
管理用户与角色分配的角色，对全部剧本有全权。
_Avoid_: 超管

**对局 / Session**：
一位玩家对某剧本的一次游玩实例，持有运行态 State + 全部 Turn；status ∈ RUNNING(1)/WIN(2)/LOSE(3)/ABANDONED(4)。同一玩家对同一剧本可有多个并行对局。
_Avoid_: 游戏局、房间

**存档 / Save**：
本系统**无显式保存**——一个**对局**本身即存档（"对局即存档"）；读档 = 拉取 state + 全部回合回放。
_Avoid_: 保存点、快照

### 约束与状态

**白名单校验 / Whitelist validation**：
对 AI 提议的跳转目标做合法性检查——目标必须落在当前节点的出边集合内、且其 condition 求真，否则拒绝并强制停留。**只约束"可达节点图"，不约束状态数值**（即"防跑偏"的精确边界，见 ADR-0001）。
_Avoid_: 转移校验、合法性校验（泛指时）

**状态 / State**：
一局对局在当前节点的运行态快照，含 flags（布尔旗标）、attributes（整数属性，如 evidence / sanity）、inventory（物品）三类；由 AI 每回合通过 stateChanges 演化，仅受 clamp 兜底。
_Avoid_: 存档（存档 = state + 全部回合的回放）

### 编剧与发布

**发布 / Publish**：
把剧本置为可玩状态（status=1）的动作；前提是通过**结构校验**。只有已发布的剧本才出现在游戏大厅、可被开局。
_Avoid_: 上线、保存（保存=草稿，≠发布）

**结构校验 / Structural validation**：
发布前对整本状态机的良构检查：有起始节点且属本剧本、非结局节点皆有出边、≥1 结局节点、分支两端合法 + condition 语法合法、从起点 BFS 可达结局。**只保证"结构良构"，不保证"真能通关"**（见 ADR-0003）。
_Avoid_: 发布校验（口语时可用，但精确指"结构"层面）

### NPC 与人格

**人设 / persona**：
NPC 的固定性格设定（性格 / 说话风格 / 动机），每回合重新注入 prompt 以保证同一 NPC 跨回合人格一致（AI 难点②）。
_Avoid_: 角色（与 RBAC 的角色 role 混淆）、设定

**knownFacts**：
下发给 ai-engine 的"该 NPC 此刻可在对白中透露的信息" = `background` +（已解锁的）`secret`；由 game-service 在拼 `GenerateRequest` 时组装。
_Avoid_: 知识、背景（背景特指 background 这一半）

**秘密 / secret**：
NPC 身上默认**不下发**的隐藏信息（如"医生是真凶"），仅当解锁条件满足才并入 knownFacts，防过早剧透。
_Avoid_: 隐情、底牌

### 记忆

**工作记忆 / recent_summary**：
存于 `t_game_state.recent_summary` 的近况滚动摘要，每回合都注入 prompt；由 game-service 简单拼接/截断维护（不额外调 LLM）。负责"刚刚发生了什么"的连续性。
_Avoid_: 上下文、history

**情节记忆 / episodic memory**：
存于 `t_memory` 向量库的离散可记事件（LLM 每回合产出的 `memoryToStore`），按"语义相似度 + importance + 新近度"召回 top-K；负责把久远但相关的往事在关键时刻"回想起来"。是长程记忆 RAG（AI 难点③）的载体。
_Avoid_: 历史、日志

## Relationships

- 一局游戏 = 玩家在**节点**间走出的一条路径，由若干次**跳转**串连
- 一个**节点**有零或多条出边**跳转**；每条**跳转**带一个 condition（白名单条目）
- 多个**回合**可发生在同一**节点**（持续**停留**）；一个**回合**内**跳转**最多发生一次
- **停留**期间**状态**持续演化；**跳转**仅在某出边 condition 满足且被放行时发生
- **白名单校验**约束**跳转**的目标节点，**不**约束**状态**的取值——AI 改不了剧情骨架，但能左右推进节奏
- 一局游戏**有且只有**经**白名单跳转**进入 `is_ending` **结局**节点才结束；引擎不另设结束规则（见 ADR-0002）
- **结构校验**只保证剧本"结构良构"，**不**保证"真能通关"——能否通关取决于 `narrative_brief` 引导 + LLM 配合（ADR-0001 的必然结果，见 ADR-0003）
- **工作记忆**保证近况连续（短期、总在 prompt 里）；**情节记忆**靠语义召回把久远往事按需唤回（长期、弱依赖，召回失败降级为空、不阻断回合）
- 一个**节点**关联零或多个在场 **NPC**；AI 只能让在场 NPC 发言，越场对白条目丢弃（NPC 版"防跑偏"）
- NPC 的**秘密**仅当 `attr.evidence` 达阈值（默认 1，< 胜利门槛 2）才并入 **knownFacts** 下发，防过早剧透（见 ADR-0004）
- 一个**玩家**可开多个**对局**（同一剧本亦可并行）；**对局**归属唯一玩家，他人访问 → 1221
- **管理员**授予 玩家/编剧/管理员 角色（roles 为数组、可多角色）；**编剧**只管自己的剧本，**管理员**全权

## Example dialogue

> **Dev**：玩家在大厅连问了管家三句话，节点变了吗？
> **设计**：没有——那是三个**回合**，都在 `node_hall` **停留**。只有当他拿到钥匙（`flag.has_key==true`）并选择进书房，才触发一次到 `node_study` 的**跳转**。

## Flagged ambiguities

- 「回合」一度被混同于「节点跳转」——已厘清：**回合 ≠ 跳转**。回合是连续交互单元，跳转是离散闸门，绝大多数回合都是停留。
- 「防跑偏」的边界——已厘清：**白名单兜"图"不兜"状态"**。AI 无法到达未建模节点，但可自由演化 flags/attributes（仅受 clamp + prompt 软约束）。见 ADR-0001。
- 「sanity=0 强制 LOSE」——已厘清：移出核心，**结局只经白名单跳转产生**、引擎无特例。sanity 仍可作为 condition 属性被编剧引用。见 ADR-0002。
- 「结构校验 = 可通关保证？」——已厘清：**否**，只保证结构良构。防软卡死靠编剧约定"每个非结局节点留一条恒走得通的退路"（`always` 边或穷尽条件），不做强制规则。见 ADR-0003。
- 「RAG 在种子规模是刚需吗？」——已厘清：**否**。种子约 10-20 回合，全摘要可塞进 prompt；RAG 定位为**演示长程记忆技术**（课程要求 + AI 难点③）并为更长剧本预留。要求：种子必须**刻意埋一个跨长间隔的可见回调**（如"保护莉莉的承诺"在 `node_confront` 被召回），否则记忆能力在 demo 里不可见。
- 「NPC 秘密怎么解锁」——已厘清：`t_npc` **无** `secret_unlock_flag` 列，§8.1 代码引用该字段的路径作废；改用 game-service 规则 `attr.evidence>=阈值`（默认 1）**统一解锁**。见 ADR-0004。
- 「双击提交返回什么」——已厘清：撞 `uk_session_turn` 唯一键时**返回上一回合已落库结果（幂等重放）**、不报 `STATE_INVALID`；前端配"提交后按钮置灰"双保险。
- 「编剧能否试玩草稿」——已厘清：基线**先发布再试玩**（开局需 `status=1`）；草稿试玩仅 stretch（开局放宽为 `status=1 或 author 本人`），核心不做。
