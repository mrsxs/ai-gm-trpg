server:
  port: 8081
spring:
  datasource:
    url: jdbc:mysql://${MYSQL_HOST:localhost}:3306/aigm_user?useUnicode=true&characterEncoding=utf8&serverTimezone=GMT%2B8&useSSL=false&allowPublicKeyRetrieval=true
    username: root
    password: "${MYSQL_PASSWORD:root}"
    driver-class-name: com.mysql.cj.jdbc.Driver
springdoc:
  swagger-ui:
    path: /doc/user.html
  api-docs:
    path: /v3/api-docs
